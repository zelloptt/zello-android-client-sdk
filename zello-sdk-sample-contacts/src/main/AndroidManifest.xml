<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zello.sdk.sample.contacts">

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.zello.sdk.sample.contacts.ContactsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <!-- Declare intents and packages that the app would like to have access to on Android R+ -->
    <queries>

        <package android:name="com.loudtalks" />

        <package android:name="net.loudtalks" />

        <package android:name="com.pttsdk" />
    </queries>
</manifest>
